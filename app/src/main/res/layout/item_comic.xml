<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <com.burak.myheroes.util.customview.TranslateDraweeView
        android:id="@+id/comicItemImageView"
        android:layout_width="60dp"
        android:layout_height="60dp"
        app:roundAsCircle="true"
        app:actualImageScaleType="centerCrop"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <TextView
        android:id="@+id/comicItemNameTextView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        app:layout_constraintTop_toTopOf="@id/comicItemImageView"
        app:layout_constraintStart_toEndOf="@+id/comicItemImageView"
        app:layout_constraintEnd_toEndOf="parent"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textColor="@color/black"
        android:maxLines="2"
        android:lines="2" />

</androidx.constraintlayout.widget.ConstraintLayout>